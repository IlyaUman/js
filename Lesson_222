var x, a;
x = Math.round(Math.random()*1000);
while (true) {
  a = prompt ("Угадай число!");
  if (a == x) {
    alert ("Верно!" + a + " " + x);
    break;
    }
    else {
      if (a < x) {
      alert ("a < x")
      }
      else {
      alert ("a > x")
      }
      }
  }
